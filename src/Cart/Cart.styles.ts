import styled from 'styled-components';
import IconButton from '@material-ui/core/IconButton';

export const Wrapper = styled.aside`
  font-family: Arial, Helvetica, sans-serif;
  width: 500px;
  padding: 50px;
  }
`;

export const BuyButton = styled(IconButton)`
  color: blue;
  border-bottom: black;
  
`

